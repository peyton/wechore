# Repository Instructions

## Verification

- Before finishing any development task that changes code, configuration, tests,
  or docs, run `mise exec -- just lint` from the repository root.
- Do not report the task as complete until lint passes. If lint cannot be run or
  cannot pass, call out the exact blocker and the command output that matters.
